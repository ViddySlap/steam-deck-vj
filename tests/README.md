# Tests

Regression tests for protocol, Deck sender/learn flows, and Windows receiver behavior.

Run from repo root:

```bash
python3 -m unittest discover -s tests -p "test_*.py"
```

If `pytest` is installed in your environment, this also works:

```bash
pytest -q
```
